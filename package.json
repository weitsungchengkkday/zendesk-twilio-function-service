{
  "name": "cti-service-zendesk",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "@twilio/runtime-handler": "1.0.2",
    "axios": "^0.27.2",
    "twilio": "^3.79"
  },
  "devDependencies": {
    "twilio-run": "^1.1.2"
  },
  "engines": {
    "node": "14"
  }
}
